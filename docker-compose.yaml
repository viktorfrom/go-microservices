version: "3.9"  # optional since v1.27.0
services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    image: hellotest123:latest
    ports:
      - "3000:3000"
    restart: always
    networks:
      - web

networks:
  web: